# Node
node_modules/
dist/
dist-ssr/
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
logs

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri
src-tauri/target/
src-tauri/gen/schemas/

# Generated icons are built-from-source via scripts/generate-icons.ps1
# We DO check them in so fresh clones can `cargo build` immediately, but
# feel free to replace them with your own brand artwork and re-commit.

# Local helper scripts produced during setup
install-buildtools.bat
